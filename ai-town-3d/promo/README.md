# 原野小镇宣传片 · 2026-09-06

成片：`../recordings/原野小镇-宣传片.mp4`，60.5 秒，1600×900，60 FPS，H.264 / AAC 48 kHz。

顺序：苏菲三维特写 → 原二维地图与居民 → 三维全景 → 周石、鲍勃、斯黛拉 → 森林与傍晚片尾。中文字幕，无设备或部署说明。二维和三维均为当前世界实机画面；没有重置存档或复活居民。

音乐由 `score.py` 通过数学合成生成，未使用商业歌曲或外部录音采样。字幕见 `promo.ass`，剪辑脚本见 `edit.mjs`。视频源文件在录制主机独立目录 `projects/ai-town-capture/promo`；录制脚本位于本项目 `capture-host/`。所有摄像机与面板调整仅在录制浏览器内生效。原二维项目的临时 Vite 进程已停止。

嘴部修复：`src/retarget-cast.js` 只迁移身体骨骼动作，面部及未映射骨骼保留局部绑定姿态并跟随父骨骼。已部署至 PCUbuntu，保留 `pre-mouth-fix-20260906.tar.gz` 回退备份。线上走路/站立动画检验：周石 22 条面部轨道、苏菲 8 条轨道，相对绑定姿态的最大四元数误差均小于 2e-7；前后截图已检查。

底层二维项目来自 AI Town；人物与场景素材的原有归属及许可参见项目素材说明。配乐原创不改变其他素材的许可义务。

## 配音修订版

用户选定 3 号云希后，新版另存为 `../recordings/原野小镇-云希男声版.mp4`。`make-yunxi.py` 使用与试听相同的 `zh-CN-YunxiNeural`（基础语速 -5%），仅对两句旁白收紧措辞以匹配原字幕含义与镜头时间；`mix-yunxi.mjs` 重新混合旁白和背景音乐，视频流直接复制，不重编码、不改大字幕及画面。女声版本保留。

新成片：`../recordings/原野小镇-配音宣传片.mp4`。原版保留。字幕 34 → 48，标题 66 → 78。使用普通话合成女声（本机 Tingting），脚本 `narration.json`；`make-voice.mjs` 生成独立语音段，`edit-voice.mjs` 对齐时间、自动压低背景音乐并混音，字幕为 `promo-voice.ass`。三维镜头使用眼睑调整上线后的重新录制画面。

法律、选举、交易已核对 PCUbuntu 原项目 `convex/society.ts` 中的 `maybeEnactLaw`、`maybeHoldElection` 及交易结算逻辑：法律采用预设提案与偏好评分表决，市长约每七个模拟日按声望、社会信任及经济等评分改选，交易包含食物、服务与税收。宣传文案没有宣称完整自主政治、真实司法体系或完全自由市场。

面部逐人检查了 8 个模型的正面、侧面、站立、行走，以及最大眨眼姿态。没有复现新的嘴角拉扯；改善重点是固定睁眼的僵硬感。`src/face-motion.js` 按 Renderpeople / Rocketbox 骨骼分别处理眼睑转动与位移，保留嘴部绑定姿态，眨眼周期错开。线上备份 `pre-face-motion-20260906.tar.gz`。`check-face-motion.mjs` 验证两种动作各 240 帧、重复调用无累积、所有变换有限，结果见 `face-regression.json`。已故居民仅检查模型，未更改其世界状态。
